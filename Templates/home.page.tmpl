{{template "base" .}}

    {{define "content"}}

        <div class="container">
            <div class= "row">
                <div class= "col">
                    <h1>My Home Page</h1>
                    <p>This is Home page<p>
                </div>    
            </div>
        </div>

{{end}}