{{template "base" .}}

    {{define "content"}}
    
        <div class = "container">
            <div class = "row">
                <div class = "col">
                    <h1>My About Page</h1>
                    <p>This is About page<p>
                </div>    
            </div>
        </div>

{{end}}